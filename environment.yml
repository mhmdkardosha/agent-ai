name: livekit-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - livekit-agents
      - livekit-plugins-silero
      - livekit-plugins-azure
      - livekit-plugins-turn-detector
      - python-dotenv
      - llama-index
      - requests
      - fastapi
      - pydantic
      - ruff
      - pytest
      - pytest-asyncio
      - httpx